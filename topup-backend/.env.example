# Database Configuration
# Path to SQLite database file (relative or absolute)
# The database contains customer acquisition (cps_tb) and forecast (forecast_df) tables
DATABASE_URL=./data/topup.db

# Chroma Vector Database
# Path to Chroma storage directory for RAG-based glossary and schema retrieval
# Used by Memory Agent to answer "What is X?" queries
CHROMA_PATH=./data/chroma

# OpenAI API Configuration
# Your OpenAI API key for LLM calls (Router, Planner, Insights agents)
# Get your API key from: https://platform.openai.com/api-keys
# Required for all agent operations
OPENAI_API_KEY=sk-your-openai-api-key-here

# Cache Configuration
# Cache implementation type:
#   - memory: In-memory LRU cache (recommended for local development)
#   - redis: Redis-based cache (requires Redis server, for production)
# Cache TTL is 10 minutes for query results
CACHE_TYPE=memory
